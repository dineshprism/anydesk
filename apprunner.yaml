version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - npm ci
    build:
      - npm run build
run:
  runtime-version: 22
  command: npm --workspace @quder/server run start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: production
